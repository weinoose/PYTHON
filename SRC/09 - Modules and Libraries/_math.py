# [Just TR descriptions available.]

import math

result = dir(math)
result = math.ceil(10.07) # Parametre olarak verilen float sayıyı bir üst integera yuvarlar.
result = math.floor(10.99) - 1 # Parametre olarak verilen float sayıyı bir üst integera yuvarlar.
result = math.factorial(100) # Parametre olarak verilen sayının faktöriyelini alır.
result = math.log(125,5) # Verilen 2 parametre sonucu logaritma hesaplayan bir metot. (Değer, Taban)
result = math.pow(3, 2) # Sıarsıyla taban ve üs olarak verilen 2 parametre sonucu üs alma işlemi yapar.
result = math.sqrt(99) # Parametre olarak gönderilen sayının karekökünü alır.
result = math.gcd(45, 70) # EBOB işlemi

print(result)