import math

result = dir(math)
# Rounds the float number given as a parameter up to the next integer. 
result = math.ceil(10.07)
# Rounds the float number given as a parameter up to the next integer. 
result = math.floor(10.99) - 1
# Factorial calculation.
result = math.factorial(100)
# Returns the values of base 'b' value number 'a'.
result = math.log(125,5)
# Returns the power 'b' of given parameter number 'a'.
result = math.pow(3, 2)
# Returns the root of the number.
result = math.sqrt(99)
# Great common divisior between number a and b.
result = math.gcd(45, 70)